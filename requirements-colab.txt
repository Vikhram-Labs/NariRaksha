# NariRaksha — minimal Colab requirements
# ⚠️  Do NOT use pip install -r directly.
# ✅  Open notebooks/NariRaksha_Colab.ipynb and run Cell 1.
#
# These are the exact packages the notebook installs.
# bitsandbytes is intentionally absent — Unsloth bundles a compatible version.

unsloth
datasets>=2.20.0
huggingface_hub>=0.23.0
pyyaml>=6.0
tqdm>=4.66